// ad_campaign_page.dart
import 'package:flutter/material.dart';

class AdCampaignPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Ad Campaign Page'),
      ),
      body: Center(
        child: Text('This is the Ad Campaign Page'),
      ),
    );
  }
}